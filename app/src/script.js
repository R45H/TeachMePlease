(function($) {
	//=require 'blocks/accordion/accordion.js'
}(jQuery));

(function($) {
	//=require 'blocks/amount/amount.js'
}(jQuery));

(function($) {
	//=require 'blocks/basket/basket.js'
}(jQuery));

/* Blocks will be inserted here */

$(function() {
	//=require 'global/global.js'
});